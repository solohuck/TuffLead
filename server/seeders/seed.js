const db = require('../config/connection');
const { User, Subscription } = require('../models');
const userSeeds = require('./userSeeds.json');
const subscriptionSeeds = require('./subscriptionSeeds.json');
